enabled: true
preview:
  enabled: true
  # Async chat waits at most this long; sync chat uses cached titles only.
  wait-millis: 1500
  timeout-millis: 3000
  cache-minutes: 60
  max-cache-entries: 512
link:
  # &6[&aLink text&6]&f; no bold or underline.
  color: "#55FF55"
  max-links: 5
kakc:
  protect-urls: true
